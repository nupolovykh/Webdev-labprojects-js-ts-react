# Third-party code

`mini-chat-server/` is **not** my work. It is a third-party educational Express +
lowdb chat server, cloned unmodified into this lab so the React front-end in
`../src/` had a real API to talk to while practising routing and queries.

It carries no licence file of its own; it is kept here for reference only, and
no authorship of it is claimed. Everything else under
`labs/10-react-routing-and-queries/` is mine.

# Lab 02 — Оживляем список дел: прикручиваем JS

> Task: add/remove items, filter by mode, sort by name; optionally edit items and persist to localStorage.

Part of the same incrementally-built app as labs 00, 01 and 05 — see **[`../05-webpack-setup/`](../05-webpack-setup)** for the finished code.

What this lab specifically added: [`src/`](../05-webpack-setup/src) (`main.js`, `taskbuilder.js`, `storage.js`, `orders.js`, `additional.js`).
